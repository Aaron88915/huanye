# huanye

静态原创角色展示站，使用原生 HTML / CSS / JavaScript 构建，可直接部署到 GitHub Pages。

## 页面

- `index.html`：主页与角色概览
- `gallery.html`：角色展示与分类筛选
- `detail.html?id=huanye`：角色详细档案（支持 `yuki`、`huanye`、`seira`、`natsu`）

将整个 `huanye` 文件夹推送到仓库后，在 GitHub Pages 中选择对应分支即可发布。
